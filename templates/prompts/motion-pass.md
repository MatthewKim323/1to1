You are doing the MOTION PASS on {{N}} sections of a 1:1 rebuild in {{PROJECT}} ({{STACK}}, `import { motion } from "motion/react"`). Read {{PROJECT}}/reference/CONVENTIONS.md first. The layout is already built and verified; do NOT change layout, sizes, text or CSS except where needed to attach motion. Only edit these files: {{FILES}} (you may read anything).

The exact motion spec:
- {{REF}}/motion/component-specs.md (§0.4 explains the mechanisms; {{SPEC_SECTIONS}} have per-element values)
- {{REF}}/motion/transitions.json (machine-readable: sections[].elements[] with mechanism / initial / animate / transition / spring / from)
- {{REF}}/motion/framer-appear.json (+ appear-by-name.json) for on-mount appears

KEY RULE: elements with mechanism "scroll-transform" (Framer "Enter" effect, trigger onInView, offset ['start end','end end']) must NOT use whileInView. They are scroll-progress-linked and reversible: progress 0 to 1 as the element travels from entering the viewport bottom to its end reaching the viewport bottom, each property interpolated from its `from` value to rest, chased by a spring (damping 60, mass 1, stiffness 500 / 300 / 250 as listed per element). Use the shared primitive in {{PROJECT}}/lib/scroll-reveal.tsx: `<ScrollReveal from={{opacity:0, y:96}} spring={FX500} perspective>` or `useScrollFx(ref, from, spring)` for elements that already have a ref (returns motion values to spread into `style`). Read that file. If a spec element needs a property the primitive lacks, extend it backward-compatibly.

Also apply verbatim: SSR appears (initial / animate / transition with the exact spring bounce / duration / delay or damping / stiffness), text effects (per-token blur appear: spring bounce 0, duration 0.4, 0.05s stagger, trigger per spec; `repeat` = replay every entry), hover / tap variants with the exact component transition constants, drag props (`drag`, `dragSnapToOrigin`, `dragTransition` values, `whileTap` cursor), variant-on-scroll switches (`useScrollVariant` with the listed threshold), count-ups (spring damping / stiffness from the spec, inView once amount .5). Replace every `// TODO(spec)` in these files with the real value and remove the comment. Where the spec is silent, keep what is there and leave a one-line comment saying the spec is silent.

Verify: `bunx tsc --noEmit -p {{PROJECT}}` passes; the dev server (http://localhost:{{PORT}}, already running, do not start another) still renders each section (`?only=<name>`); `1to1 shot "http://localhost:{{PORT}}/?only=<name>" out.png --scrollto <y>` at two or three offsets shows elements moving in from the specified `from` values and settling at rest, and scrolling back up reverses them. Compare rest state against {{REF}}/capture/desktop/sections/*.png to make sure nothing regressed. Origin blackout: no comment, name or value you add may say where this came from, and `1to1 blackout {{PROJECT}}` must stay CLEAN. Section desktop y-ranges: {{Y_RANGES}}. No em dashes anywhere.

Reply with: per section, the list of elements you wired and the exact values used, anything in the spec you could not implement and why, and the final tsc result.
