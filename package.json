{
  "name": "1to1",
  "version": "1.0.0",
  "description": "Clone anything. Extract a live site into ground truth (DOM, per-breakpoint CSS, revealed layouts, 60fps frames, Framer motion internals), rebuild it, and verify to the pixel.",
  "type": "module",
  "bin": {
    "1to1": "./bin/1to1"
  },
  "scripts": {
    "1to1": "bun src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "node-html-parser": "^7.0.1",
    "playwright": "^1.48.0",
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  },
  "trustedDependencies": [
    "sharp"
  ]
}
